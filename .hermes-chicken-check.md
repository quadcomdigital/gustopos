# VERIFICA (sola lettura, NON modificare): dove Chicken appare ancora con kg / unita incoerente

## Stato dati attuale (verificato in DB, tenant ten_26ed333e-9dbd-43f7-85b1-fe57054e9e6f)
- Ingrediente Chicken (i_bdad78750836): unit base KG, stock 3.00, is_stock_tracked true
- Conversione: inventory_unit_conversions pz->kg factor 0.08 (presente, from_unit minuscolo)
- Menu items con componente Chicken (menu_item_components): AGOSTO BURGER, CHICKEN, CHICKEN WRAP, PISTACCHIO BURGER -> tutti quantity 1 unit pz

## Sospetto utente
L'utente segnala che su Chicken vede ancora pz o un problema di refresh dati o di tabelle di conversione. Va capito DOVE (POS? backoffice? food cost? modale prodotto?) Chicken appare con unita diversa da pz, o con valore stale (es. 1 kg) dopo il PATCH.

## Cosa verificare (sola lettura)
1. Come il frontend risolve e mostra l'unita del componente Chicken nei menu items:
   - apps/web/src/components/inventory/MenuItemsTab.tsx (lista MENU, colonna recipe: mostra unit del componente o dell ingrediente?)
   - apps/web/src/components/POSView.tsx / POSProductModal.tsx / ModifierModal.tsx (POS: cosa mostra per il componente Chicken?)
   - eventuale resolvedRecipes / mapBomItems / mapMenuItemsAdmin: se la UI usa ingredientUnit (kg) invece di component.unit (pz) per il display
2. Se il PATCH /api/menu con components [Chicken 1 pz] persiste e la GET /api/menu o /api/data restituisce ancora kg per quei componenti (possibile mapping stale o schema che normalizza a kg)
3. Verifica API reale: login (x-tenant-id ten_26ed333e-9dbd-43f7-85b1-fe57054e9e6f, staff s_10b46124-1c34-4b59-91f2-e4afaf6b0dbc pin 1065), poi GET /api/menu e cerca i 4 prodotti: controlla il campo recipe/ingredients per Chicken (deve essere quantity 1 unit pz)
4. Eventuale problema di conversione: quando un ordine consuma Chicken 1 pz, il calcolo stock usa factor 0.08? (app.repository.ts consumption) - confermare che il path BoM/modifier/menu component applichi la conversione.

## Output richiesto
- Dove esattamente appare Chicken con unita errata/stale (file:riga o schermata)
- Causa (display usa ingredientUnit? mapping? cache?) 
- NON modificare nulla: solo report.
