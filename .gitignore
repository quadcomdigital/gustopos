node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local-only artifacts (added at the "complete stack" squash):
/.opencode/
/.mimocode/
/anydesk.deb
/BUILD_HASHES.txt
/PLAN-*.md

# API signing secret (provisioned by deployment; never commit or serve publicly):
/apps/api/signing/
# Web signing private key: must NEVER be committed or served from public/ (only digital-certificate.txt is public):
/apps/web/public/signing/private-key.pem

# Print-agent release artifacts (generated — publish via make publish-downloads):
/apps/print-agent-go/bin/
/apps/web/public/downloads/

# Graphify analysis output (regenerated tooling artifacts):
/graphify-out/

# Local DB dumps (never commit)
/backups/
